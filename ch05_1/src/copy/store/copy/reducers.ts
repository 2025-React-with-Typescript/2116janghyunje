import * as T from './types'
const initalState: T.State = {}
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const reducer = (state: T.State = initalState, action: T.Actions) => {
    return state
}